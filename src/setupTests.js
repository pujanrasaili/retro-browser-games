// jest-dom adds custom jest matchers for asserting on DOM nodes,
// e.g. expect(element).toBeInTheDocument(). Automatically picked up
// by react-scripts test since it's named setupTests.js in src/.
import '@testing-library/jest-dom';
